__pycache__/
*.pyc
.venv/
venv/

# Node / Next.js (dashboard)
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Runtime state, not source (repo-root only; do not ignore dashboard routes)
db/*.sqlite3
logs/*.log
# Legacy local-only output dirs (pipeline writes under published/ now)
/digests/
/wiki/
/curriculum/
/labs/
/transcripts/
# published/ is tracked: category/date markdown synced to GitHub

# Credentials
.env

# Deploy-time data snapshot (scripts/deploy-dashboard.sh), never committed
dashboard/.data/

# Forecast note — 2026-07-16

First forecast after the initial ingest. There is not yet a multi-week trailing baseline in this database, so velocity here is **within-window concentration and corroboration shape**, not week-over-week deltas. Revisit once nightly ingest has built a longer series.

## Agent evaluation protocols (continual / stress / harness)

**Signal.** 13 tier-1 curated items landed in topic `Agent Evaluation` in a single ingest day, including continual-learning optimizer evals (Terminal-Bench 2.0), deep-search stress tests, composable agent graphs, cross-device agent benchmarks, and verifier cascades. That is a narrow-subfield cluster, not a diffuse "agents are hot" claim.

**Why leading.** Teaching material that only shows one-shot benchmark lift will look outdated once continual and regression-aware protocols become the default comparison. If this cluster keeps arriving across weeks, curriculum should privilege evaluation design over tool demos.

**Worth teaching if it keeps accelerating.** A durable unit on compounding/transfer metrics and in-loop regression control (already scaffolded as unit #2).

## LLM-as-judge stimulus integrity

**Signal.** 5 curated items under `LLM Judges`, including a structural test-oracle failure mode for synthetic judge corpora, audio-judge protocol shortcuts, and cross-rubric generalization. Smaller count than agent eval, but the oracle paper is a mechanism claim, not a brand claim.

**Why leading.** Internal eval harnesses are proliferating; bad synthetic negatives silently invent "bias" results. That failure mode compounds as more teams automate judging.

**Worth teaching if it keeps accelerating.** Corpus audit checklists before trusting judge scores (unit #1).

## Agentic coding adoption at project scale

**Signal.** Tier-1 paper on early adoption of agentic coding tools across 2,361 GitHub repos / 25k+ agentic PRs, plus high same-day star velocity on coding-adjacent repos (`github/copilot-sdk` 62 stars today; `Nutlope/hallmark` 3,181 stars today on ~10.5k total, a steep daily fraction).

**Why leading.** Project-level governance (review norms, ownership, revert patterns) lags individual Copilot demos by months. Star spikes alone are weak; paired with the adoption study they suggest teaching workflow integration soon.

**Worth teaching if it keeps accelerating.** A frontier one-shot on human-agent PR norms and what to measure (accept/revert, not LOC generated).

## Quiet / not listed

No workshop/CFP language in this window. Broad buckets (`Practitioner Notes`, `Research Misc`) are volume, not velocity, and were not promoted to the watchlist.
